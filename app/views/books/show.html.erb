<div class="show">

<a class="suc">
	<% flash.each do |key, value| %>
	<div class="alert alert-<%= key %>" >
		<%= value %>
	</div>
<% end %>
<%= yield %>
</a>


<h4 class="t-1">Title: <%= @book.title %></h4>

<h4 class="t-2">Body: <%= @book.body %></h4>

</div>

<div class="show-c">
<%= link_to "Edit", edit_book_path(@book.id), class: 'edit_#{@book.id}' %>

  
<%= link_to "Back", books_path, class: 'back' %>
